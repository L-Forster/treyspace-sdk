# Save exact versions in package-lock.json
save-exact=true

# Automatically install peer dependencies
legacy-peer-deps=false

# Engine strict - fail if Node version doesn't match
engine-strict=true

# Prefer online for faster downloads
prefer-online=true

# Update notifier
update-notifier=true

# Progress
progress=true
